# esbuild-typescript-figma-plugin

This is a template plugin for Figma that uses TypeScript and esbuild to build the plugin.
It uses no framework, just plain TypeScript and HTML / CSS and DOM manipulation.